/* `training-utils.js` pide tres íconos para el saludo de la pantalla del
   atleta ("Buenos días" con su sol). En el servidor no se dibuja nada, así que
   aquí van vacíos: el resto del archivo se usa tal cual, sin copiarlo a mano. */
export const Sunrise = null;
export const Sun = null;
export const Moon = null;
